--- a/install/arch-linux/limine-mkinitcpio-hook/usr/share/libalpm/scripts/limine-mkinitcpio-install
+++ b/install/arch-linux/limine-mkinitcpio-hook/usr/share/libalpm/scripts/limine-mkinitcpio-install
@@ -54,6 +54,7 @@ resolve_uki_prefix
 # Add kernel directories owned by a package.
 add_kernel_package() {
 	local package="$1" path
+	[[ -z "$package" || "$package" == */* ]] && return 1
 	local found=false
 	while IFS= read -r path; do
 		KERNEL_DIRS+=("$path")
@@ -79,7 +80,7 @@ collect_kernels() {
 			# Convert the extramodules path to the kernel directory.
 			path="/${line%%/extramodules/*}"
 			KERNEL_DIRS+=("$path")
-		elif add_kernel_package "$line"; then
+		elif [[ "$line" != */* ]] && add_kernel_package "$line"; then
 			continue
 		elif [[ "$line" != usr/lib/modules/* ]]; then
 			rebuild_all=true
