provider: generic
channel: stable
# Omarchy Spark: the vendor self-updater would fetch a .deb and run apt; this
# package is updated with pacman instead, so the feed points at nothing.
url: http://127.0.0.1:1/omarchy-spark-updates-disabled
updaterCacheDirName: nvidia-ai-workbench-updater
