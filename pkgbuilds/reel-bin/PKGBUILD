# Maintainer: Pixygon <hello@pixygon.io>
# Generated by pearl omarchy — do not hand-edit; re-run to refresh.
pkgname=reel-bin
pkgver=1.15.1
pkgrel=1
pkgdesc='Reel — play anything, edit everything: a native media player, editor and screen recorder'
arch=('x86_64')
url='https://reel.pixygon.io'
license=('MIT')
depends=('mpv' 'ffmpeg')
provides=('reel')
conflicts=('reel')
options=(!strip !debug)
source=("${pkgname}-${pkgver}.tar.gz::https://pixygontech.b-cdn.net/releases/reel/v1.15.1/reel-linux-x86_64.tar.gz")
sha256sums=('4f588288fb7b964a4a4f439ba9e5d8feb35a650ee9bd0674922c181477e084e8')

package() {
    install -Dm755 "${srcdir}/reel" "${pkgdir}/usr/bin/reel"

    install -Dm644 /dev/stdin "${pkgdir}/usr/share/applications/reel.desktop" <<'DESKTOP'
[Desktop Entry]
Type=Application
Name=Reel
Comment=Reel — play anything, edit everything: a native media player, editor and screen recorder
Exec=reel %F
Terminal=false
Categories=AudioVideo;Player;Video;AudioVideoEditing;
DESKTOP
}
