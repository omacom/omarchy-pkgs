schema = 1

[package]
name = "some-tool"
description = "A tool built from a signed tag"
upstream = "https://gitlab.com/group/subgroup/some-tool"
license = "GPL-3.0-or-later"
surface = "binary"
architectures = ["x86_64"]

[source]
kind = "tag"
version = "1.2.3"

[requester]
relationship = "upstream_author"
