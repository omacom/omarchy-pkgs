schema = 1

[package]
name = "vendor-thing"
description = "A vendor download requested as a signed binary"
upstream = "https://dl.example.com/linux/thing"
license = "MIT"
surface = "binary"
architectures = ["x86_64"]

[source]
kind = "vendor"

[requester]
relationship = "user"
