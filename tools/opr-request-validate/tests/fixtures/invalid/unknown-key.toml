schema = 1

[package]
name = "sneaky"
description = "Carries a key nobody designed"
upstream = "https://github.com/example/sneaky"
license = "MIT"
surface = "binary"
architectures = ["x86_64"]
depends = ["glibc"]

[source]
kind = "release"

[requester]
relationship = "user"
