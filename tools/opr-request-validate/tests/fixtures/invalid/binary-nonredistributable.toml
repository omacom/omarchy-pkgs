schema = 1

[package]
name = "propriety"
description = "Asks us to host bits we may not redistribute"
upstream = "https://github.com/example/propriety"
license = "LicenseRef-Vendor-ToS"
surface = "binary"
architectures = ["x86_64"]

[source]
kind = "release"

[requester]
relationship = "user"
