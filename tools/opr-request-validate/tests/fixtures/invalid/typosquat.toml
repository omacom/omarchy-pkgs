schema = 1

[package]
name = "ghosttty"
description = "One keystroke from a package that already exists"
upstream = "https://github.com/example/ghosttty"
license = "MIT"
surface = "binary"
architectures = ["x86_64"]

[source]
kind = "release"

[requester]
relationship = "user"
