schema = 1

[package]
name = "thing; rm -rf / #"
description = "A name that would be dangerous if it were ever shell"
upstream = "https://github.com/example/thing"
license = "MIT"
surface = "binary"
architectures = ["x86_64"]

[source]
kind = "release"

[requester]
relationship = "user"
